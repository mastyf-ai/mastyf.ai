# Mastyf Security Gateway v0.1.0-RC1 Acceptance Checklist

## 1. Product Positioning & Boundary Scope

> **Mastyf Security Gateway v0.1.0 is a production-pilot security gateway for AI-agent tool execution, using deterministic capability and information-flow controls with a frozen V6 neural auditor as a semantic fallback.**

### Scope Invariants & Boundaries
- **Research Artifact**: V6 Boundary-Sharpened SFT checkpoint is **strictly frozen and immutable** at revision `d59a6aa01f9139dff106146addb04109afa69c03` on Hugging Face (`Rudraneel93/mastyf-guard-1.5b-v2-boundary-sharpened`).
- **Authorization Invariant**:
  $$\text{Authority}(\text{Final}) \subseteq \text{Authority}(\text{CBAC}) \cap \text{Authority}(\text{DIFC})$$
  $$\text{Final} = \text{ALLOW} \implies \text{CBAC} = \text{ALLOW} \land \text{DIFC} = \text{ALLOW}$$
- **Execution Suspension Invariant**:
  $$\text{Decision} \in \{\text{BLOCK}, \text{ESCALATE}\} \implies \text{BackendToolInvocations} = 0$$

---

## 2. Five Release Candidate Acceptance Gates

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                   v0.1.0-RC1 ACCEPTANCE GATES                               │
│                                                                             │
│  [Gate 1] Clean Distribution Lifecycle (install → doctor → verify → start)  │
│  [Gate 2] Security & Invariant Suite (38/38 passing, zero fail-open)        │
│  [Gate 3] Protocol Enforcement (MCP JSON-RPC proxy + zero backend leak)     │
│  [Gate 4] Performance Characterization (3 μs fast-path vs empirical V6)     │
│  [Gate 5] Failure & Chaos Invariance (timeouts, malformed, corruption)      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### Gate 1: Clean Distribution Lifecycle

*Objective*: Verify end-to-end operator workflow from clean installation to daemon startup.

- [x] **Repository Separation**:
  - Research Checkpoint: `Rudraneel93/mastyf-guard-1.5b-v2-boundary-sharpened` (Frozen V6)
  - Gateway Runtime: `Rudraneel93/mastyf-gateway` (v0.1.0 Software distribution)
  - Historical Baseline: `Rudraneel93/mastyf-guard-1.5b` (v2.0 reproducibility)
- [x] **Installation Automation**:
  - POSIX Installer: [`install/install.sh`](file:///Users/rudraneeldas/Projects/mastyf-full/mastyf_gateway/install/install.sh) (Linux / macOS)
  - Windows Installer: [`install/install.ps1`](file:///Users/rudraneeldas/Projects/mastyf-full/mastyf_gateway/install/install.ps1) (PowerShell)
  - Docker Compose: [`docker-compose.yml`](file:///Users/rudraneeldas/Projects/mastyf-full/mastyf_gateway/docker-compose.yml) & [`Dockerfile`](file:///Users/rudraneeldas/Projects/mastyf-full/mastyf_gateway/Dockerfile)
- [x] **CLI Command Sequence**:
  ```bash
  mastyf init               # Scaffolds ~/.mastyf, config, policy, log dirs
  mastyf doctor             # Validates Python 3.10+, PyTorch MPS/CUDA, ports, permissions
  mastyf model install v6   # Pins revision d59a6aa01f9139dff106146addb04109afa69c03
  mastyf verify             # Verifies reproducible build metadata & cryptographic chain
  mastyf status             # Inspects live component health (CBAC, DIFC, AIA, MCP)
  mastyf self-test          # Runs 4-path live canary asserting 0 executions on deny/escalate
  mastyf start              # Launches REST & MCP Proxy daemon on port 8787
  ```

---

### Gate 2: Security Invariants Suite

*Objective*: Execute automated invariant regression suite asserting mathematical authority monotonicity and fail-closed state.

- [x] **Test Results**: **38/38 automated tests passed (100% green)**
- [x] **Authority Monotonicity**:
  - Full $2 \times 2 \times 4 \times 2 \times 2 = 64$ permutation decision space verified: AIA cannot override CBAC or DIFC denial under any input.
- [x] **Fail-Closed Verification**:
  - Missing policy $\to$ `BLOCK`
  - Unknown tool $\to$ `BLOCK`
  - Unauthorized principal $\to$ `BLOCK`
  - Schema / argument constraint violation $\to$ `BLOCK`
  - Null byte (`\x00`) injection $\to$ `BLOCK`
  - Untrusted taint to exfiltration sink $\to$ `BLOCK`
  - Auditor timeout $\to$ `ESCALATE` (with 0 backend execution)
  - Auditor malformed output $\to$ `ESCALATE` (with 0 backend execution)

---

### Gate 3: Protocol Enforcement (MCP Proxy)

*Objective*: Assert that the Model Context Protocol (MCP) JSON-RPC proxy transparently intercepts tool calls without bypass.

- [x] **MCP JSON-RPC Handling**: Intercepts `tools/call` JSON-RPC messages and evaluates through CBAC $\to$ DIFC $\to$ AIA $\to$ Arbiter pipeline.
- [x] **Zero-Leak Invariant**:
  - 100 concurrent attack requests fired across CBAC domain violations, unauthorized principals, and DIFC exfiltrations resulted in **0 backend server executions**.
- [x] **First-Class Escalation**:
  - MCP proxy converts `ESCALATE` into JSON-RPC error code `-32002`, strictly suspending tool dispatch and recording the audit event.

---

### Gate 4: Performance & Latency Characterization

*Objective*: Accurately characterize reference monitor throughput and distinguish in-process fast-path from empirical V6 neural latency.

- [x] **Deterministic Pre-Filter Throughput**:
  - CBAC + DIFC Fast-Path: **$\sim 3.0\ \mu\text{s}$ per decision** ($>330,000\text{ req/s}$ in-process).
  - Pre-filtering eliminates $67.0\%$ of structural, constraint, and taint attacks before neural compute.
- [x] **Empirical V6 Neural Latencies**:
  - InjecAgent benchmark workload: $267.7\text{ ms } P_{50} \ / \ 331.0\text{ ms } P_{95}$
  - AgentDojo benchmark workload: $951.5\text{ ms } P_{50} \ / \ 2259.8\text{ ms } P_{95}$
- [x] **Effective Amortized Latency Model**:
  $$T_{\text{effective}} = (1 - \alpha) \cdot T_{\text{fast}} + \alpha \cdot (T_{\text{fast}} + T_{\text{AIA}})$$
  $$T_{\text{effective}} = 0.67(0.003\text{ ms}) + 0.33(0.003\text{ ms} + 267.7\text{ ms}) \approx 88.4\text{ ms}$$
  *(Empirical framework delivers $\sim 3\times$ reduction in required neural GPU load).*

---

### Gate 5: Failure & Chaos Invariance

*Objective*: Assert authorization survival under hostile faults and concurrency stress.

- [x] **Auditor Timeout Injection**: Hard deadline supervisor safely converts AIA hangs into `ESCALATE` within configured SLO budget.
- [x] **Decoder Fuzzing**: Malformed JSON, corrupted markdown, truncated tokens, and prompt escapes all resolve to deterministic `ESCALATE` / `BLOCK`.
- [x] **State Isolation**: Concurrent interleaved sessions across multiple tenants maintain strict zero-cross-contamination taint boundaries.

---

## 3. Reproducible Build Metadata Binding & Signed Trust Chain

```text
Gateway Version           : 0.1.0 (v0.1.0-rc1)
Gateway Source Git SHA    : 4331fa92305bf9c4d25785089e52e46b9a896d8e
V6 Hugging Face Revision  : d59a6aa01f9139dff106146addb04109afa69c03 (IMMUTABLE)
V6 Model Repository       : Rudraneel93/mastyf-guard-1.5b-v2-boundary-sharpened
Release Signature         : Ed25519 / SHA256-SigChain (mastyf-release-authority@mastyf.ai)
Release Manifest File     : release_manifest.json
Security Manifest SHA     : 7531dc298aad187e86af7b13200726090c74d98fdde3ccb31d7e20a82bb336b8
Package Checksum File     : checksums.txt (SHA-256)
Build Timestamp           : 2026-09-05T21:46:14Z
Build Environment         : CPython 3.10+ (x86_64 / arm64 / Linux / macOS / Windows NT)
Trust Chain Status        : VERIFIED & CRYPTOGRAPHICALLY PINNED
```

---

## 4. Clean-Machine RC Validation Artifact

The complete clean-machine installation, diagnostics, model pinning, verification, canary self-test, 38-test invariant regression suite, and concurrency load sweep were executed in an isolated test environment and logged:

* **Validation Record**: [`reports/v0.1.0-rc1-clean-machine-validation.txt`](file:///Users/rudraneeldas/Projects/mastyf-full/mastyf_gateway/reports/v0.1.0-rc1-clean-machine-validation.txt)
* **Gate Status**: **ALL 5 GATES PASSED (100% GREEN)**

