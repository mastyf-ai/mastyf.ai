apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-guardian.fullname" . }}
  labels:
    {{- include "mcp-guardian.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-guardian.selectorLabels" . | nindent 4 }}